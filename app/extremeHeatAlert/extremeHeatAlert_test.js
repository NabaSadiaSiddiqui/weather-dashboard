'use strict';

describe('weatherDashboard.extremeHeatAlert module', function() {

  beforeEach(module('weatherDashboard.extremeHeatAlert'));

  describe('extremeHeatAlert controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var extremeHeatAlertCtrl = $controller('ExtremeHeatAlertCtrl');
      expect(extremeHeatAlertCtrl).toBeDefined();
    }));

  });
});
