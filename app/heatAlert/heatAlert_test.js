'use strict';

describe('weatherDashboard.heatAlert module', function() {

  beforeEach(module('weatherDashboard.heatAlert'));

  describe('heatAlert controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var heatAlertCtrl = $controller('HeatAlertCtrl');
      expect(heatAlertCtrl).toBeDefined();
    }));

  });
});
